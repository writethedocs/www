<svg viewBox="0 0 660 288" fill="none" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <style type="text/css">
      <![CDATA[
        @import url(/_static/survey/css/2025-svg.css);
      ]]>
    </style>
  </defs>

  <g id="2025-salary-gender-gap-v2" class="scale-gender-gap">
    <g class="chart-header">
      <g class="chart-title">
        <text x="20" y="35" class="title-text">Median salary by gender identity (USD)</text>
        <text x="20" y="55" class="subtitle-text">Employees — regions with 30+ respondents per gender shown</text>
      </g>

      <g class="legend">
        <g class="legend-row">
          <g class="legend-item">
            <rect class="legend-item-container" x="514" y="20" width="65"/>
            <circle class="women" cx="527" cy="31" r="4"/>
            <text class="legend-label" x="535" y="35">Women</text>
          </g>
          <g class="legend-item">
            <rect class="legend-item-container" x="589" y="20" width="51"/>
            <circle class="men" cx="602" cy="31" r="4"/>
            <text class="legend-label" x="610" y="35">Men</text>
          </g>
        </g>
      </g>
    </g>

    <g class="chart-area bar-chart-horizontal scale-gender-gap" transform="translate(0 34)">
      <g class="labels" font-size="10" text-anchor="end">
        <text font-weight="700"><tspan x="88" y="80">Worldwide</tspan></text>
        <text font-weight="700"><tspan x="88" y="124">North America</tspan></text>
        <text font-weight="700"><tspan x="88" y="168">Europe</tspan></text>
      </g>

      <g class="tracks">
        <rect class="track" x="96" y="62" width="542" height="28"/>
        <rect class="track" x="96" y="106" width="542" height="28"/>
        <rect class="track" x="96" y="150" width="542" height="28"/>
      </g>

      <g class="ranges">
        <line class="gap" x1="322.3" y1="76" x2="361.6" y2="76"/>
        <line class="gap" x1="453.7" y1="120" x2="537.7" y2="120"/>
        <line class="gap" x1="153.1" y1="164" x2="237.7" y2="164"/>
      </g>

      <g class="gap-labels" text-anchor="middle">
        <text class="gap"><tspan x="341.9" y="58">+7.9%</tspan></text>
        <text class="gap"><tspan x="495.7" y="102">+13.4%</tspan></text>
        <text class="gap"><tspan x="195.4" y="146">+25.8%</tspan></text>
      </g>

      <g class="points">
        <circle cx="322.3" cy="76" r="8" class="women"/>
        <circle cx="361.6" cy="76" r="8" class="men"/>
        <circle cx="453.7" cy="120" r="8" class="women"/>
        <circle cx="537.7" cy="120" r="8" class="men"/>
        <circle cx="153.1" cy="164" r="8" class="women"/>
        <circle cx="237.7" cy="164" r="8" class="men"/>
      </g>

      <g class="value-labels" text-anchor="middle">
        <text class="median-value"><tspan x="322.3" y="98">$91,750</tspan></text>
        <text class="median-value"><tspan x="361.6" y="98">$99,000</tspan></text>
        <text class="median-value"><tspan x="453.7" y="142">$116,000</tspan></text>
        <text class="median-value"><tspan x="537.7" y="142">$131,500</tspan></text>
        <text class="median-value"><tspan x="153.1" y="186">$60,526</tspan></text>
        <text class="median-value"><tspan x="237.7" y="186">$76,137</tspan></text>
      </g>

      <g class="x-axis" text-anchor="middle">
        <line x1="96" y1="214" x2="638" y2="214" stroke="#F0F0F0" stroke-width="1"/>
        <g class="axis-labels">
          <text font-size="10"><tspan x="96" y="234">$50K</tspan></text>
          <text font-size="10"><tspan x="231.5" y="234">$75K</tspan></text>
          <text font-size="10"><tspan x="367" y="234">$100K</tspan></text>
          <text font-size="10"><tspan x="502.5" y="234">$125K</tspan></text>
          <text font-size="10"><tspan x="638" y="234">$150K</tspan></text>
        </g>
      </g>
    </g>
  </g>
  <script type="application/ecmascript"><![CDATA[
    (function () {
      var root = document.querySelector('svg > g[id], g[id]');
      if (!root) {
        return;
      }

      var ignoredClasses = {
        'legend-item-container': true,
        'legend-label': true,
        'legend-item': true,
        'legend-row': true,
        'slice': true,
        'track': true,
        'tracks': true,
        'bars': true,
        'data-series': true,
        'series-lines': true,
        'chart-area': true,
        'chart-header': true,
        'chart-title': true,
        'axis-labels': true,
        'gridlines': true,
        'value-labels-inner': true,
        'value-labels-outer': true,
        'chart-color-1': true,
        'chart-color-2': true,
        'chart-color-3': true,
        'chart-color-4': true,
        'chart-color-5': true,
        'chart-color-6': true
      };

      function firstSeriesClass(element) {
        if (!element || !element.classList) {
          return null;
        }

        for (var i = 0; i < element.classList.length; i += 1) {
          var className = element.classList[i];
          if (!ignoredClasses[className]) {
            return className;
          }
        }

        return null;
      }

      function legendSeries(item) {
        var swatch = item.querySelector('circle[class], rect[class]:not(.legend-item-container), line[class], polyline[class], path[class], ellipse[class], polygon[class]');
        return firstSeriesClass(swatch);
      }

      var legendItems = Array.prototype.slice.call(root.querySelectorAll('.legend-item')).filter(function (item) {
        return !!legendSeries(item);
      });

      if (legendItems.length === 0) {
        return;
      }

      var seriesNames = [];
      legendItems.forEach(function (item) {
        var series = legendSeries(item);
        item.setAttribute('data-series', series);
        if (!item.hasAttribute('tabindex')) {
          item.setAttribute('tabindex', '0');
        }
        item.style.cursor = 'pointer';

        var container = item.querySelector('rect.legend-item-container');
        if (container) {
          container.style.transition = 'opacity 220ms ease-in-out, stroke 220ms ease-in-out, stroke-width 220ms ease-in-out';
        }

        var label = item.querySelector('text.legend-label');
        if (label) {
          label.style.transition = 'opacity 220ms ease-in-out';
        }

        if (seriesNames.indexOf(series) === -1) {
          seriesNames.push(series);
        }
      });

      var elementsBySeries = {};
      seriesNames.forEach(function (series) {
        elementsBySeries[series] = [];
      });

      Array.prototype.forEach.call(root.querySelectorAll('*'), function (element) {
        if (element.closest('.legend')) {
          return;
        }

        var series = null;
        if (element.hasAttribute && element.hasAttribute('data-series')) {
          var dataSeries = element.getAttribute('data-series');
          if (seriesNames.indexOf(dataSeries) !== -1) {
            series = dataSeries;
          }
        }

        if (!series) {
          series = firstSeriesClass(element);
        }

        if (!series) {
          var ancestor = element.parentElement;
          while (ancestor && ancestor !== root && !series) {
            if (ancestor.hasAttribute && ancestor.hasAttribute('data-series')) {
              var ancestorDataSeries = ancestor.getAttribute('data-series');
              if (seriesNames.indexOf(ancestorDataSeries) !== -1) {
                series = ancestorDataSeries;
                break;
              }
            }

            var ancestorSeries = firstSeriesClass(ancestor);
            if (ancestorSeries && seriesNames.indexOf(ancestorSeries) !== -1) {
              series = ancestorSeries;
              break;
            }

            ancestor = ancestor.parentElement;
          }
        }

        if (!series || seriesNames.indexOf(series) === -1) {
          return;
        }

        elementsBySeries[series].push(element);
        element.style.transition = 'opacity 220ms ease-in-out, fill-opacity 220ms ease-in-out, stroke-opacity 220ms ease-in-out';
      });

      function clearActiveSeries() {
        legendItems.forEach(function (item) {
          item.style.removeProperty('opacity');
          var container = item.querySelector('rect.legend-item-container');
          if (container) {
            container.style.removeProperty('stroke');
            container.style.removeProperty('stroke-width');
          }
        });

        seriesNames.forEach(function (series) {
          elementsBySeries[series].forEach(function (element) {
            element.style.removeProperty('opacity');
          });
        });
      }

      function setActiveSeries(series) {
        var isSingleSeries = seriesNames.length === 1;

        legendItems.forEach(function (item) {
          var isActive = item.getAttribute('data-series') === series;
          item.style.opacity = isActive ? '1' : '0.4';

          var container = item.querySelector('rect.legend-item-container');
          if (container) {
            container.style.stroke = isActive ? 'var(--color-text-dark)' : '';
            container.style.strokeWidth = isActive ? '1.5' : '';
          }
        });

        seriesNames.forEach(function (name) {
          var isActive = name === series;
          elementsBySeries[name].forEach(function (element) {
            if (isSingleSeries) {
              element.style.fillOpacity = isActive ? '1' : '0.25';
              element.style.strokeOpacity = isActive ? '1' : '0.25';
              element.style.removeProperty('opacity');
            } else {
              element.style.opacity = isActive ? '1' : '0.25';
              element.style.removeProperty('fill-opacity');
              element.style.removeProperty('stroke-opacity');
            }
          });
        });
      }

      legendItems.forEach(function (item) {
        var series = item.getAttribute('data-series');

        item.addEventListener('mouseenter', function () {
          setActiveSeries(series);
        });

        item.addEventListener('mouseleave', clearActiveSeries);

        item.addEventListener('focus', function () {
          setActiveSeries(series);
        });

        item.addEventListener('blur', clearActiveSeries);
      });
    }());
  ]]></script>
</svg>