<svg viewBox="0 0 660 305" fill="none" xmlns="http://www.w3.org/2000/svg">

  <defs>
    <style type="text/css">
      <![CDATA[
        @import url(/_static/survey/css/2025-svg.css);
      ]]>
    </style>
    <filter id="label-bg" x="-15%" y="-40%" width="130%" height="180%">
      <feMorphology in="SourceAlpha" operator="dilate" radius="3" result="expanded"/>
      <feGaussianBlur in="expanded" stdDeviation="5" result="blurred"/>
      <feComponentTransfer in="blurred" result="rounded">
        <feFuncA type="linear" slope="20" intercept="-5"/>
      </feComponentTransfer>
      <feFlood flood-color="white" flood-opacity="0.8" result="color"/>
      <feComposite in="color" in2="rounded" operator="in" result="bg"/>
      <feMerge>
        <feMergeNode in="bg"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>

  <g id="2025-feedback-usability-v2">
    <g class="chart-header">
      <g class="chart-title">
        <text x="20" y="35" class="title-text">Survey feedback</text>
        <text x="20" y="55" class="subtitle-text">Form/website is usable</text>
      </g>

      <g class="legend scale-feedback-likert">
        <g class="legend-row">
          <g class="legend-item">
            <rect class="legend-item-container" x="376" y="20" width="109"/>
            <circle class="strongly-disagree" cx="389" cy="31"/>
            <text class="legend-label" x="397" y="35">Strongly disagree</text>
          </g>
          <g class="legend-item">
            <rect class="legend-item-container" x="495" y="20" width="70"/>
            <circle class="disagree" cx="508" cy="31"/>
            <text class="legend-label" x="516" y="35">Disagree</text>
          </g>
          <g class="legend-item">
            <rect class="legend-item-container" x="575" y="20" width="65"/>
            <circle class="neutral" cx="588" cy="31"/>
            <text class="legend-label" x="596" y="35">Neutral</text>
          </g>
        </g>
        <g class="legend-row">
          <g class="legend-item">
            <rect class="legend-item-container" x="478" y="52" width="59"/>
            <circle class="agree" cx="491" cy="63"/>
            <text class="legend-label" x="499" y="67">Agree</text>
          </g>
          <g class="legend-item">
            <rect class="legend-item-container" x="547" y="52" width="93"/>
            <circle class="strongly-agree" cx="560" cy="63"/>
            <text class="legend-label" x="568" y="67">Strongly agree</text>
          </g>
        </g>
      </g>
    </g>

    <g class="chart-area donut-chart scale-feedback-likert" transform="translate(0 22) translate(330 262) scale(1.2) translate(-330 -262)">
      <path class="slice disagree" d="M180 262 A150 150 0 0 1 180.036 258.702 L270.015 260.681 A60 60 0 0 0 270 262 Z"/>
      <path class="slice neutral" d="M180.036 258.702 A150 150 0 0 1 181.496 240.865 L270.599 253.546 A60 60 0 0 0 270.015 260.681 Z"/>
      <path class="slice agree" d="M181.496 240.865 A150 150 0 0 1 277.861 121.353 L309.145 205.741 A60 60 0 0 0 270.599 253.546 Z"/>
      <text font-size="14" text-anchor="middle" class="value-labels-inner"><tspan x="248" y="200">34.2%</tspan></text>
      <path class="slice strongly-agree" d="M277.861 121.353 A150 150 0 0 1 480 262 L390 262 A60 60 0 0 0 309.145 205.741 Z"/>
      <text font-size="14" text-anchor="middle" class="value-labels-inner"><tspan x="390" y="180">61.3%</tspan></text>
    
    </g>
  </g>

  <script type="application/ecmascript"><![CDATA[
    (function () {
      var root = document.querySelector('svg > g[id], g[id]');
      if (!root) {
        return;
      }

      var ignoredClasses = {
        'legend-item-container': true,
        'legend-label': true,
        'legend-item': true,
        'legend-row': true,
        'slice': true,
        'track': true,
        'tracks': true,
        'bars': true,
        'data-series': true,
        'series-lines': true,
        'chart-area': true,
        'chart-header': true,
        'chart-title': true,
        'axis-labels': true,
        'gridlines': true,
        'value-labels-inner': true,
        'value-labels-outer': true
      };

      function firstSeriesClass(element) {
        if (!element || !element.classList) {
          return null;
        }

        for (var i = 0; i < element.classList.length; i += 1) {
          var className = element.classList[i];
          if (!ignoredClasses[className]) {
            return className;
          }
        }

        return null;
      }

      function legendSeries(item) {
        var swatch = item.querySelector('circle[class], rect[class]:not(.legend-item-container), line[class], polyline[class], path[class], ellipse[class], polygon[class]');
        return firstSeriesClass(swatch);
      }

      var legendItems = Array.prototype.slice.call(root.querySelectorAll('.legend-item')).filter(function (item) {
        return !!legendSeries(item);
      });

      if (legendItems.length <= 1) {
        return;
      }

      var seriesNames = [];
      legendItems.forEach(function (item) {
        var series = legendSeries(item);
        item.setAttribute('data-series', series);
        if (!item.hasAttribute('tabindex')) {
          item.setAttribute('tabindex', '0');
        }
        item.style.cursor = 'pointer';

        var container = item.querySelector('rect.legend-item-container');
        if (container) {
          container.style.transition = 'opacity 160ms ease, stroke 160ms ease, stroke-width 160ms ease';
        }

        var label = item.querySelector('text.legend-label');
        if (label) {
          label.style.transition = 'opacity 160ms ease';
        }

        if (seriesNames.indexOf(series) === -1) {
          seriesNames.push(series);
        }
      });

      var elementsBySeries = {};
      seriesNames.forEach(function (series) {
        elementsBySeries[series] = [];
      });

      Array.prototype.forEach.call(root.querySelectorAll('*'), function (element) {
        if (element.closest('.legend')) {
          return;
        }

        var series = null;
        if (element.hasAttribute && element.hasAttribute('data-series')) {
          var dataSeries = element.getAttribute('data-series');
          if (seriesNames.indexOf(dataSeries) !== -1) {
            series = dataSeries;
          }
        }

        if (!series) {
          series = firstSeriesClass(element);
        }

        if (!series || seriesNames.indexOf(series) === -1) {
          return;
        }

        elementsBySeries[series].push(element);
        element.style.transition = 'opacity 160ms ease, fill-opacity 160ms ease, stroke-opacity 160ms ease';
      });

      function clearActiveSeries() {
        legendItems.forEach(function (item) {
          item.style.removeProperty('opacity');
          var container = item.querySelector('rect.legend-item-container');
          if (container) {
            container.style.removeProperty('stroke');
            container.style.removeProperty('stroke-width');
          }
        });

        seriesNames.forEach(function (series) {
          elementsBySeries[series].forEach(function (element) {
            element.style.removeProperty('opacity');
          });
        });
      }

      function setActiveSeries(series) {
        legendItems.forEach(function (item) {
          var isActive = item.getAttribute('data-series') === series;
          item.style.opacity = isActive ? '1' : '0.4';

          var container = item.querySelector('rect.legend-item-container');
          if (container) {
            container.style.stroke = isActive ? 'var(--color-text-dark)' : '';
            container.style.strokeWidth = isActive ? '1.5' : '';
          }
        });

        seriesNames.forEach(function (name) {
          var isActive = name === series;
          elementsBySeries[name].forEach(function (element) {
            element.style.opacity = isActive ? '1' : '0.25';
          });
        });
      }

      legendItems.forEach(function (item) {
        var series = item.getAttribute('data-series');

        item.addEventListener('mouseenter', function () {
          setActiveSeries(series);
        });

        item.addEventListener('mouseleave', clearActiveSeries);

        item.addEventListener('focus', function () {
          setActiveSeries(series);
        });

        item.addEventListener('blur', clearActiveSeries);
      });
    }());
  ]]></script>
</svg>