<svg viewBox="0 0 660 340" fill="none" xmlns="http://www.w3.org/2000/svg">

  <defs>
      <style type="text/css">
        <![CDATA[
          @import url(/_static/survey/css/2025-svg.css);

          #2025-hours-worked-employees-v2 .bar-value-label,
          #2025-hours-worked-employees-v2 .bar-value-label * {
            font-size: 10px;
            font-weight: 600;
            pointer-events: none;
          }

          #2025-hours-worked-employees-v2 .bar-value-label {
            transition: opacity 220ms ease-in-out, font-size 220ms ease-in-out, fill-opacity 220ms ease-in-out;
          }

          #2025-hours-worked-employees-v2 .bar-value-label.hover-only {
            opacity: 0;
          }

          #2025-hours-worked-employees-v2 .bar-value-label.outside,
          #2025-hours-worked-employees-v2 .bar-value-label.outside * {
            fill: var(--color-text-dark);
            font-size: 10px;
            font-weight: 600;
          }
        ]]>
      </style>
      <filter id="label-bg" x="-15%" y="-40%" width="130%" height="180%">
        <feMorphology in="SourceAlpha" operator="dilate" radius="3" result="expanded"/>
        <feGaussianBlur in="expanded" stdDeviation="5" result="blurred"/>
        <feComponentTransfer in="blurred" result="rounded">
          <feFuncA type="linear" slope="20" intercept="-5"/>
        </feComponentTransfer>
        <feFlood flood-color="white" flood-opacity="0.8" result="color"/>
        <feComposite in="color" in2="rounded" operator="in" result="bg"/>
        <feMerge>
          <feMergeNode in="bg"/>
          <feMergeNode in="SourceGraphic"/>
        </feMerge>
      </filter>
    </defs>

  <g id="2025-hours-worked-employees-v2" data-color-count="1">
    <g class="chart-header">
      <g class="chart-title">
        <text x="20" y="35" class="title-text">Weekly hours worked</text>
        <text x="20" y="55" class="subtitle-text">Employee respondents (675)</text>
      </g>

      <g class="legend scale-single-series">
        <g class="legend-row">
          <g class="legend-item">
            <rect class="legend-item-container" x="561" y="20" width="79"/>
            <circle class="series-a chart-color-1" cx="574" cy="31"/>
            <text class="legend-label" x="582" y="35">Employees</text>
          </g>
        </g>
      </g>
    </g>

    <g class="chart-area bar-chart-vertical scale-single-series" transform="translate(0,-20)">
      <g class="y-axis">
        <g class="gridlines">
          <path d="M20 314.9H640"/>
          <path d="M20 240.7H640"/>
          <path d="M20 166.3H640"/>
        </g>
        <g class="axis-labels">
          <text font-size="10"><tspan x="25" y="317.9">0%</tspan></text>
          <text font-size="10"><tspan x="20" y="243.7">25%</tspan></text>
          <text font-size="10"><tspan x="20" y="169.3">50%</tspan></text>
        </g>
      </g>

        <g class="data-series">
          <g class="a">
            <g class="axis-labels">
              <text font-size="12" text-anchor="middle" data-bar-key="hours-1-20"><tspan x="101.9" y="332.8">1–20</tspan></text>
              <text font-size="12" text-anchor="middle" data-bar-key="hours-21-30"><tspan x="200.9" y="332.8">21–30</tspan></text>
              <text font-size="12" text-anchor="middle" data-bar-key="hours-31-40"><tspan x="299.9" y="332.8">31–40</tspan></text>
              <text font-size="12" text-anchor="middle" data-bar-key="hours-41-50"><tspan x="398.9" y="332.8">41–50</tspan></text>
              <text font-size="12" text-anchor="middle" data-bar-key="hours-51-60"><tspan x="497.9" y="332.8">51–60</tspan></text>
              <text font-size="12" text-anchor="middle" data-bar-key="hours-60-plus"><tspan x="596.9" y="332.8">60+</tspan></text>
            </g>
            <g class="bars chart-color-1 series-a">
              <rect x="62.0" y="313.7" width="80.0" height="1.8" rx="2" data-bar-key="hours-1-20"/>
              <rect x="161.0" y="312.0" width="80.0" height="3.6" rx="2" data-bar-key="hours-21-30"/>
              <rect x="260.0" y="115.0" width="80.0" height="200.5" rx="2" data-bar-key="hours-31-40"/>
              <rect x="359.0" y="227.4" width="80.0" height="88.1" rx="2" data-bar-key="hours-41-50"/>
              <rect x="458.0" y="308.4" width="80.0" height="7.1" rx="2" data-bar-key="hours-51-60"/>
              <rect x="557.0" y="315.5" width="80.0" height="0.0" rx="2" data-bar-key="hours-60-plus"/>
            </g>
            <g class="value-labels">
              <text class="value-labels-outer bar-value-label outside hover-only" text-anchor="middle" data-bar-key="hours-1-20"><tspan x="102" y="305">0.4%</tspan></text>
              <text class="value-labels-outer bar-value-label outside hover-only" text-anchor="middle" data-bar-key="hours-21-30"><tspan x="201" y="303">1.0%</tspan></text>
              <text class="value-labels-inner bar-value-label inside" text-anchor="middle" data-bar-key="hours-31-40" filter="url(#label-bg)"><tspan x="300" y="140">67.0%</tspan></text>
              <text class="value-labels-inner bar-value-label inside" text-anchor="middle" data-bar-key="hours-41-50" filter="url(#label-bg)"><tspan x="399" y="251">29.3%</tspan></text>
              <text class="value-labels-outer bar-value-label outside hover-only" text-anchor="middle" data-bar-key="hours-51-60"><tspan x="498" y="300">2.2%</tspan></text>
              <text class="value-labels-outer bar-value-label outside hover-only" text-anchor="middle" data-bar-key="hours-60-plus"><tspan x="597" y="307">0.0%</tspan></text>
            </g>
          </g>
        </g>
      </g>
    </g>

  <script type="application/ecmascript"><![CDATA[
    (function () {
      var root = document.querySelector('svg > g[id], g[id]');
      if (!root) {
        return;
      }

      var legendItem = root.querySelector('.legend-item');
      var legendContainer = legendItem ? legendItem.querySelector('rect.legend-item-container') : null;
      var legendLabel = legendItem ? legendItem.querySelector('text.legend-label') : null;
      var bars = Array.prototype.slice.call(root.querySelectorAll('.bars rect[data-bar-key]'));
      var axisLabels = Array.prototype.slice.call(root.querySelectorAll('.data-series > .a > .axis-labels text[data-bar-key]'));
      var valueLabels = Array.prototype.slice.call(root.querySelectorAll('.bar-value-label[data-bar-key]'));

      if (!legendItem || bars.length === 0 || axisLabels.length === 0 || valueLabels.length === 0) {
        return;
      }

      if (!legendItem.hasAttribute('tabindex')) {
        legendItem.setAttribute('tabindex', '0');
      }
      legendItem.style.cursor = 'pointer';

      if (legendContainer) {
        legendContainer.style.transition = 'opacity 220ms ease-in-out, stroke 220ms ease-in-out, stroke-width 220ms ease-in-out';
      }

      if (legendLabel) {
        legendLabel.style.transition = 'opacity 220ms ease-in-out';
      }

      function setLabelTypography(label, size, weight) {
        label.style.fontSize = size;
        label.style.fontWeight = weight;
        Array.prototype.forEach.call(label.querySelectorAll('tspan'), function (span) {
          span.style.fontSize = size;
          span.style.fontWeight = weight;
        });
      }

      bars.forEach(function (bar) {
        bar.style.cursor = 'pointer';
        bar.style.transition = 'fill-opacity 220ms ease-in-out, opacity 220ms ease-in-out';
      });

      axisLabels.forEach(function (label) {
        label.style.cursor = 'pointer';
        label.style.transition = 'opacity 220ms ease-in-out';
      });

      valueLabels.forEach(function (label) {
        if (!label.style.transition) {
          label.style.transition = 'opacity 220ms ease-in-out, font-size 220ms ease-in-out, fill-opacity 220ms ease-in-out';
        }
      });

      var activeMode = null;
      var activeBarKey = null;

      function renderState() {
        var isSeriesActive = activeMode === 'series';

        if (legendContainer) {
          legendContainer.style.stroke = isSeriesActive ? 'var(--color-text-dark)' : '';
          legendContainer.style.strokeWidth = isSeriesActive ? '1.5' : '';
        }

        bars.forEach(function (bar) {
          var isBarActive = activeMode === 'bar' && bar.getAttribute('data-bar-key') === activeBarKey;
          if (isSeriesActive || isBarActive) {
            bar.style.fillOpacity = '1';
          } else {
            bar.style.removeProperty('fill-opacity');
          }
        });

        valueLabels.forEach(function (label) {
          var isHoverOnly = label.classList.contains('hover-only');
          var isLabelActive = isSeriesActive || (activeMode === 'bar' && label.getAttribute('data-bar-key') === activeBarKey);

          if (isLabelActive) {
            label.style.opacity = '1';
            setLabelTypography(label, '14px', '700');
            label.style.fillOpacity = '1';
          } else {
            label.style.opacity = isHoverOnly ? '0' : '';
            setLabelTypography(label, '10px', '600');
            label.style.removeProperty('fill-opacity');
          }
        });
      }

      function activateSeries() {
        activeMode = 'series';
        activeBarKey = null;
        renderState();
      }

      function clearSeries() {
        if (activeMode === 'series') {
          activeMode = null;
          activeBarKey = null;
          renderState();
        }
      }

      function activateBar(barKey) {
        activeMode = 'bar';
        activeBarKey = barKey;
        renderState();
      }

      function clearBar(barKey) {
        if (activeMode === 'bar' && activeBarKey === barKey) {
          activeMode = null;
          activeBarKey = null;
          renderState();
        }
      }

      legendItem.addEventListener('mouseenter', activateSeries);
      legendItem.addEventListener('mouseleave', clearSeries);
      legendItem.addEventListener('focus', activateSeries);
      legendItem.addEventListener('blur', clearSeries);

      bars.forEach(function (bar) {
        var barKey = bar.getAttribute('data-bar-key');

        bar.addEventListener('mouseenter', function () {
          activateBar(barKey);
        });

        bar.addEventListener('mouseleave', function () {
          clearBar(barKey);
        });
      });

      axisLabels.forEach(function (label) {
        var barKey = label.getAttribute('data-bar-key');

        label.addEventListener('mouseenter', function () {
          activateBar(barKey);
        });

        label.addEventListener('mouseleave', function () {
          clearBar(barKey);
        });
      });

      renderState();
    }());
  ]]></script>
</svg>
