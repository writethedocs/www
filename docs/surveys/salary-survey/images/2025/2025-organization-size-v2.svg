<svg viewBox="0 0 660 436" fill="none" xmlns="http://www.w3.org/2000/svg">

  <defs>
    <style type="text/css">
      <![CDATA[
        @import url(/_static/survey/css/2025-svg.css);
        #2025-organization-size-v2 .bar-value-label,
        #2025-organization-size-v2 .bar-value-label * {
          font-size: 10px;
          font-weight: 600;
          pointer-events: none;
        }
        #2025-organization-size-v2 .bar-value-label {
          transition: opacity 220ms ease-in-out, font-size 220ms ease-in-out, fill-opacity 220ms ease-in-out;
        }
        #2025-organization-size-v2 .bar-value-label.hover-only {
          opacity: 0;
        }
        #2025-organization-size-v2 .bar-value-label.outside,
        #2025-organization-size-v2 .bar-value-label.outside * {
          fill: var(--color-text-dark);
          font-size: 10px;
          font-weight: 600;
        }
      ]]>
    </style>
    <filter id="label-bg" x="-15%" y="-40%" width="130%" height="180%">
      <feMorphology in="SourceAlpha" operator="dilate" radius="3" result="expanded"/>
      <feGaussianBlur in="expanded" stdDeviation="5" result="blurred"/>
      <feComponentTransfer in="blurred" result="rounded">
        <feFuncA type="linear" slope="20" intercept="-5"/>
      </feComponentTransfer>
      <feFlood flood-color="white" flood-opacity="0.8" result="color"/>
      <feComposite in="color" in2="rounded" operator="in" result="bg"/>
      <feMerge>
        <feMergeNode in="bg"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>

  <g id="2025-organization-size-v2" data-color-count="1">
    <g class="chart-header">
      <g class="chart-title">
        <text x="20" y="35" class="title-text">Organization size</text>
        <text x="20" y="55" class="subtitle-text">Number of employees</text>
      </g>

      <g class="legend scale-single-series">
        <g class="legend-row">
          <g class="legend-item">
            <rect class="legend-item-container" x="552" y="20" width="88"/>
            <circle class="series-a chart-color-1" cx="565" cy="31"/>
            <text class="legend-label" x="573" y="35">Respondents</text>
          </g>
        </g>
      </g>
    </g>

    <g class="chart-area bar-chart-vertical scale-single-series" transform="translate(0 -20)">

      <g class="y-axis">

        <g class="gridlines">
          <path d="M68 369.5H620" stroke="#F0F0F0"/>
          <path d="M68 299.5H620" stroke="#F0F0F0"/>
          <path d="M68 229.5H620" stroke="#F0F0F0"/>
          <path d="M68 159.5H620" stroke="#F0F0F0"/>
        </g>

        <g class="axis-labels">
          <text font-size="10"><tspan x="45" y="372">0%</tspan></text>
          <text font-size="10"><tspan x="40" y="302">10%</tspan></text>
          <text font-size="10"><tspan x="40" y="232">20%</tspan></text>
          <text font-size="10"><tspan x="40" y="162">30%</tspan></text>
        </g>

      </g>

      <g class="data-series">

        <g class="a">

          <g class="axis-labels">
            <text font-size="10" data-bar-key="org-1-10"><tspan x="98" y="386">1-10</tspan></text>
            <text font-size="10" data-bar-key="org-11-50"><tspan x="174" y="386">11-50</tspan></text>
            <text font-size="10" data-bar-key="org-51-100"><tspan x="251" y="386">51-100</tspan></text>
            <text font-size="10" data-bar-key="org-101-1000"><tspan x="324" y="386">101-1000</tspan></text>
            <text font-size="10" data-bar-key="org-1001-10000"><tspan x="397" y="386">1001-10,000</tspan></text>
            <text font-size="10" data-bar-key="org-10001-100000"><tspan x="469" y="386">10,001-100,000</tspan></text>
            <text font-size="10" data-bar-key="org-100000-plus"><tspan x="560" y="386">100,000+</tspan></text>
          </g>

          <g class="bars chart-color-1 series-a">
            <!-- 1-10: 1.2% → top=362 -->
            <rect class="series-a chart-color-1" x="71"  y="362" width="73" height="8"   rx="2" data-bar-key="org-1-10"/>
            <!-- 11-50: 4.9% → top=335 -->
            <rect class="series-a chart-color-1" x="150" y="335" width="73" height="35"  rx="2" data-bar-key="org-11-50"/>
            <!-- 51-100: 7.4% → top=318 -->
            <rect class="series-a chart-color-1" x="229" y="318" width="72" height="52"  rx="2" data-bar-key="org-51-100"/>
            <!-- 101-1,000: 34.4% → top=127 -->
            <rect class="series-a chart-color-1" x="308" y="127" width="72" height="243" rx="2" data-bar-key="org-101-1000"/>
            <!-- 1,001-10,000: 29.4% → top=162 -->
            <rect class="series-a chart-color-1" x="387" y="162" width="72" height="208" rx="2" data-bar-key="org-1001-10000"/>
            <!-- 10,001-100,000: 14.4% → top=268 -->
            <rect class="series-a chart-color-1" x="465" y="268" width="73" height="102" rx="2" data-bar-key="org-10001-100000"/>
            <!-- 100,000+: 8.3% → top=311 -->
            <rect class="series-a chart-color-1" x="544" y="311" width="73" height="59"  rx="2" data-bar-key="org-100000-plus"/>
          </g>

          <g class="value-labels">
            <text class="value-labels-outer bar-value-label outside hover-only" text-anchor="middle" data-bar-key="org-1-10"><tspan x="108" y="354">1.2%</tspan></text>
            <text class="value-labels-inner bar-value-label inside" text-anchor="middle" data-bar-key="org-11-50" filter="url(#label-bg)"><tspan x="187" y="360">4.9%</tspan></text>
            <text class="value-labels-inner bar-value-label inside" text-anchor="middle" data-bar-key="org-51-100" filter="url(#label-bg)"><tspan x="265" y="343">7.4%</tspan></text>
            <text class="value-labels-inner bar-value-label inside" text-anchor="middle" data-bar-key="org-101-1000" filter="url(#label-bg)"><tspan x="344" y="152">34.4%</tspan></text>
            <text class="value-labels-inner bar-value-label inside" text-anchor="middle" data-bar-key="org-1001-10000" filter="url(#label-bg)"><tspan x="423" y="187">29.4%</tspan></text>
            <text class="value-labels-inner bar-value-label inside" text-anchor="middle" data-bar-key="org-10001-100000" filter="url(#label-bg)"><tspan x="502" y="293">14.4%</tspan></text>
            <text class="value-labels-inner bar-value-label inside" text-anchor="middle" data-bar-key="org-100000-plus" filter="url(#label-bg)"><tspan x="581" y="336">8.3%</tspan></text>
          </g>

        </g>

      </g>

      
    </g>
  </g>

  <script type="application/ecmascript"><![CDATA[
    (function () {
      var root = document.querySelector('svg > g[id], g[id]');
      if (!root) {
        return;
      }

      var legendItem = root.querySelector('.legend-item');
      var legendContainer = legendItem ? legendItem.querySelector('rect.legend-item-container') : null;
      var legendLabel = legendItem ? legendItem.querySelector('text.legend-label') : null;
      var bars = Array.prototype.slice.call(root.querySelectorAll('.bars rect[data-bar-key]'));
      var axisLabels = Array.prototype.slice.call(root.querySelectorAll('.data-series > .a > .axis-labels text[data-bar-key]'));
      var valueLabels = Array.prototype.slice.call(root.querySelectorAll('.bar-value-label[data-bar-key]'));

      if (!legendItem || bars.length === 0 || axisLabels.length === 0 || valueLabels.length === 0) {
        return;
      }

      if (!legendItem.hasAttribute('tabindex')) {
        legendItem.setAttribute('tabindex', '0');
      }
      legendItem.style.cursor = 'pointer';

      if (legendContainer) {
        legendContainer.style.transition = 'opacity 220ms ease-in-out, stroke 220ms ease-in-out, stroke-width 220ms ease-in-out';
      }

      if (legendLabel) {
        legendLabel.style.transition = 'opacity 220ms ease-in-out';
      }

      function setLabelTypography(label, size, weight) {
        label.style.fontSize = size;
        label.style.fontWeight = weight;
        Array.prototype.forEach.call(label.querySelectorAll('tspan'), function (span) {
          span.style.fontSize = size;
          span.style.fontWeight = weight;
        });
      }

      bars.forEach(function (bar) {
        bar.style.cursor = 'pointer';
        bar.style.transition = 'fill-opacity 220ms ease-in-out, opacity 220ms ease-in-out';
      });

      axisLabels.forEach(function (label) {
        label.style.cursor = 'pointer';
        label.style.transition = 'opacity 220ms ease-in-out';
      });

      valueLabels.forEach(function (label) {
        if (!label.style.transition) {
          label.style.transition = 'opacity 220ms ease-in-out, font-size 220ms ease-in-out, fill-opacity 220ms ease-in-out';
        }
      });

      var activeMode = null;
      var activeBarKey = null;

      function renderState() {
        var isSeriesActive = activeMode === 'series';

        if (legendContainer) {
          legendContainer.style.stroke = isSeriesActive ? 'var(--color-text-dark)' : '';
          legendContainer.style.strokeWidth = isSeriesActive ? '1.5' : '';
        }

        bars.forEach(function (bar) {
          var isBarActive = activeMode === 'bar' && bar.getAttribute('data-bar-key') === activeBarKey;
          if (isSeriesActive || isBarActive) {
            bar.style.fillOpacity = '1';
          } else {
            bar.style.removeProperty('fill-opacity');
          }
        });

        valueLabels.forEach(function (label) {
          var isHoverOnly = label.classList.contains('hover-only');
          var isLabelActive = isSeriesActive || (activeMode === 'bar' && label.getAttribute('data-bar-key') === activeBarKey);

          if (isLabelActive) {
            label.style.opacity = '1';
            setLabelTypography(label, '14px', '700');
            label.style.fillOpacity = '1';
          } else {
            label.style.opacity = isHoverOnly ? '0' : '';
            setLabelTypography(label, '10px', '600');
            label.style.removeProperty('fill-opacity');
          }
        });
      }

      function activateSeries() {
        activeMode = 'series';
        activeBarKey = null;
        renderState();
      }

      function clearSeries() {
        if (activeMode === 'series') {
          activeMode = null;
          activeBarKey = null;
          renderState();
        }
      }

      function activateBar(barKey) {
        activeMode = 'bar';
        activeBarKey = barKey;
        renderState();
      }

      function clearBar(barKey) {
        if (activeMode === 'bar' && activeBarKey === barKey) {
          activeMode = null;
          activeBarKey = null;
          renderState();
        }
      }

      legendItem.addEventListener('mouseenter', activateSeries);
      legendItem.addEventListener('mouseleave', clearSeries);
      legendItem.addEventListener('focus', activateSeries);
      legendItem.addEventListener('blur', clearSeries);

      bars.forEach(function (bar) {
        var barKey = bar.getAttribute('data-bar-key');

        bar.addEventListener('mouseenter', function () {
          activateBar(barKey);
        });

        bar.addEventListener('mouseleave', function () {
          clearBar(barKey);
        });
      });

      axisLabels.forEach(function (label) {
        var barKey = label.getAttribute('data-bar-key');

        label.addEventListener('mouseenter', function () {
          activateBar(barKey);
        });

        label.addEventListener('mouseleave', function () {
          clearBar(barKey);
        });
      });

      renderState();
    }());
  ]]></script>
</svg>
